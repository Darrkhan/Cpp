using namespace std;
#if !defined(_graphe_)
#define _graphe_

#include "VIP.hpp"
const int GRAPH_SIZE = 6;
const int INFINI = 9999999;
template<typename T>

/*class graphe{
    private:
      std::vector<T> vecSommets;
      int arcs [GRAPH_SIZE][GRAPH_SIZE];
      int shortpath [GRAPH_SIZE][GRAPH_SIZE];
      int pathpoint [GRAPH_SIZE][GRAPH_SIZE];

    public:

};*/
#endif
int algo_dijkstra(vector<T>vecSommets);
